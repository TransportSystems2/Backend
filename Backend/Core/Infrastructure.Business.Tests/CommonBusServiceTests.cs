namespace TransportSystems.Infrastructure.Business.Tests
{
    public class CommonBusServiceTests
    {
        /*
        [Fact]
        public void AddOrderResultHandleAddedOrderEvent()
        {
            var orderServiceMock = new Mock<IOrderService>();
            var notificationService = new NotificationService();
            var commonBusService = new CommonBusService(orderServiceMock);
            orderServiceMock.Raise(mock => mock.AddedOrder += null, new EventArgs());

            notificationService.ModeratorNoticeWasSent

            Assert.
        }
        */
    }
}